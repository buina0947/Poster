export interface Post {
    _id: string;
    title: string;
    description: string;
    postTitle?: string;
    postDescription?: string;
    postAuthor?: string;
    author?: string; 
    id?: string; 
}
